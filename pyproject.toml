[project]
name = "spawnet"
version = "0.1.0"
description = "Tiny Python SPA host and packager demo."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "pyinstaller>=6.16.0",
]

[tool.uv]
package = false
